"""Embed stage tasks — Phase 2 stub.

No Celery tasks are defined here yet. This module exists so the Celery
worker can import it without raising ModuleNotFoundError. Tasks will be
added in Phase 2 (knowledge units + graph build).
"""
from __future__ import annotations
